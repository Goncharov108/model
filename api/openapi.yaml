openapi: 3.1.0
info:
  title: model API (placeholder)
  version: "0"
paths:
  /health:
    get:
      summary: Проверка живости сервиса
      responses:
        "200":
          description: Сервис отвечает
          content:
            application/json:
              schema:
                type: object
                required: [ok, service]
                properties:
                  ok:
                    type: boolean
                  service:
                    type: string
                  version:
                    type: string
