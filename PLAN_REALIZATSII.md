# План реализации репозитория `model`

Документ фиксирует дорожную карту от текущего локального UI до более широкой системы из канонов (CANON_SECONDARY_MASTER, CANON_PROJECT_MASTER). Источник истины по процессу — `SWOD_ZAKONOV.txt`; этот файл — **операционный план** (можно править по мере уточнения приоритетов).

**Статус обновления:** при выполнении фазы отмечайте чекбоксы и при необходимости добавляйте дату в скобках.

---

## Текущее состояние (зафиксировано)

- [x] Локальный веб-стек в `web/`: Vite, React, TypeScript, Tailwind, маршруты Поток / Структуры / План / Оркестр.
- [x] Анализ текста, импорт внешнего JSON (`ExternalAnalysisV1`), канон-2 и канон-3 в слой.
- [x] Режим «Оркестр»: роли, задачи, persist `model-orchestra-v1`, экспорт/импорт JSON.
- [x] Свод законов, канонические тексты, имя репозитория `model`.

---

## Фаза 1 — Надёжность и дисциплина кода (короткий цикл)

Цель: не раздувать функциональность, а закрепить то, что уже есть.

- [x] Документ `PLAN_REALIZATSII.md` в корне и ссылка из `web/README.md`.
- [x] Модульные тесты (Vitest) для `parseOrchestraImport` / `buildOrchestraExport` и для `parsePlanningImport` / `buildPlanningExport`.
- [x] Скрипты `npm run test` и `npm run test:watch` в `web/package.json`.
- [x] Расширить тестами `parseExternalAnalysisJson` (по мере стабилизации формата).
- [ ] CI (GitHub Actions или аналог): `lint`, `test`, `build` на push — когда появится удалённый репозиторий с CI.

---

## Фаза 2 — UX рабочего места «Поток»

- [ ] Явная связка экрана «Поток» с файлом канона в репозитории (подсказки/ссылки только на пути в Git, без автозагрузки секретов).
- [ ] Улучшение доступности: фокус-ловушки в модалках (если появятся), контраст кнопок.
- [ ] Экспорт «снимка сессии» одним архивом (опционально): мастер-текст не включать — только то, что уже в браузере.

---

## Фаза 3 — Доменная модель в коде (прототип без бэкенда)

Цель: перевести словарь из канона-3 в типы и фабрики в `web/src`, без обязательного сервера.

- [ ] Черновик `domain/` или `model/`: сущности слой, субъект, объект, отношение (типы + пустые редьюсеры или in-memory store).
- [ ] Связь «абзац анализа ↔ тег темы из JSON» для навигации по импорту.

---

## Фаза 4 — Отдельный API-слой (после выбора стека)

По канону-3 перспектива — NestJS + Postgres + Keycloak + OpenFGA; **решение не зафиксировано** в SWOD до явного утверждения.

- [ ] Репозиторий или каталог `api/` с health-check и одной read-only ручкой.
- [ ] Схема миграций и заготовка под RLS (документ + пустые миграции).
- [ ] Контракт OpenAPI между `web` и `api`.

---

## Фаза 5 — Интеграции (очередь)

- [ ] OIDC (Keycloak) в dev-контуре.
- [ ] OpenFGA: модель отношений для одного сценария (например «гость у хозяина»).
- [ ] Вложения через S3-совместимое хранилище (MinIO) — после фазы 4.

---

## Как вести план

1. Закрывать пункты только после merge в основную ветку или явного «готово» от владельца.
2. Крупные отклонения от канона — сначала правка или дополнение SWOD / канона, потом код.
3. Параллельная работа агентов в IDE — планировать на вкладке **Оркестр** (`/orchestra`), экспорт JSON при необходимости.
