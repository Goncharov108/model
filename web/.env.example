# Скопируйте в web/.env.local (не коммитить). Тот же Client ID, что GOOGLE_CLIENT_ID на API.
VITE_GOOGLE_CLIENT_ID=

# Базовый URL API (prod: /api, dev: прокси vite → localhost:3847)
VITE_API_BASE=/api
