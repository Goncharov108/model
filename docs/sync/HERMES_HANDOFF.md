# Передача работы: Hermes (Telegram) → Дас (Cursor)

Единый журнал того, что сделано с **Hermes** на VPS, чтобы в Cursor не искать по чату TG.

## Как писать (Hermes на сервере)

После **значимой** сессии (коммит, деплой, правки в `api/` / `web/` / `docs/`, настройка сервиса):

1. Дописать блок ниже по шаблону.
2. Закоммитить в ветку **`hermes/work`** (не в `main` без согласия владельца).
3. По возможности: `git push origin hermes/work` (если настроен deploy key).

### Шаблон блока

```markdown
## YYYY-MM-DD HH:MM (MSK) — краткий заголовок

- **Ветка:** hermes/work @ <hash>
- **Сделано:** …
- **Файлы:** …
- **Деплой / сервисы:** да/нет, что именно
- **Нужно в Cursor:** что проверить / слить / откатить
- **Риски:** конфликты с локальной работой, секреты, не трогать …
```

---

## 2026-05-17 22:38 (MSK) — Этап 10 для «ТГ заметок»: модальный предпросмотр и тихий импорт без window.confirm

- **Ветка:** `hermes/work` @ `HEAD`
- **Сделано:**
  - убран `window.confirm` из импорта пресетов;
  - добавлен модальный `ConfirmDialog` с детальным предпросмотром: сколько добавится, сколько будет переименовано, сколько пропущено;
  - в модалке показываются примеры переименований (до 5), чтобы заранее видеть конфликтные имена;
  - импорт теперь «тихий»: выбор файла только открывает модалку, применение происходит по кнопке «Импортировать».
- **Файлы:**
  - `web/src/routes/MasterAdminIncomingWorkspace.tsx`
  - `docs/sync/HERMES_HANDOFF.md`
- **Деплой / сервисы:** нет
- **Нужно в Cursor:**
  - визуально проверить мобильный UX модалки (перенос длинных строк и комфорт чтения);
  - при желании расширить модалку отдельным списком всех конфликтов (сейчас выводим примеры).
- **Риски:** общий проектный build всё ещё может упираться в старый `HermesTerminalChat.tsx` (вне области ТГ заметок).

## 2026-05-17 22:32 (MSK) — Этап 9 для «ТГ заметок»: предпросмотр, отчёт импорта, мягкая валидация и undo

- **Ветка:** `hermes/work` @ `HEAD`
- **Сделано:**
  - режимы импорта в UI оставлены на русском в пользовательской формулировке: «Добавить к текущим» и «Заменить пользовательские»;
  - в store добавлен предпросмотр импорта `previewImportPresets` (показывает будущие имена, конфликты/переименования, количество пропущенных невалидных);
  - `importPresets` расширен до подробного результата: `added`, `renamed`, `skipped`, `mode`;
  - реализована мягкая валидация: невалидные пресеты пропускаются, валидные импортируются;
  - добавлен откат последнего импорта `undoLastImport` (один шаг назад по пресетам);
  - в UI добавлены: confirm-предпросмотр перед применением, пост-импорт отчёт и кнопка «Отменить последний импорт».
- **Файлы:**
  - `web/src/store/telegramNotesStore.ts`
  - `web/src/store/telegramNotesStore.test.ts`
  - `web/src/routes/MasterAdminIncomingWorkspace.tsx`
  - `docs/sync/HERMES_HANDOFF.md`
- **Деплой / сервисы:** нет
- **Нужно в Cursor:**
  - UX-проверка сценария подтверждения (окно confirm + блок предпросмотра после выбора файла);
  - решить, нужен ли постоянный лимит/пагинация списка предпросмотра (сейчас показываются первые 8, остальное счётчиком).
- **Риски:** `npm run build` по проекту всё ещё может блокироваться старым `HermesTerminalChat.tsx` (xterm/типы), к этапу 9 не относится.

## 2026-05-17 20:41 (MSK) — Этап 8 для «ТГ заметок»: merge-импорт пресетов и защита от дублей имён

- **Ветка:** `hermes/work` @ `HEAD`
- **Сделано:**
  - импорт пресетов расширен режимами: `merge` (добавить к текущим) и `replace` (заменить пользовательские);
  - в merge-режиме текущие пользовательские пресеты сохраняются, импортированные добавляются;
  - добавлена защита от дублей имён (включая регистр): при конфликте имя автоматически становится `Название (2)`, `Название (3)` и т.д.;
  - импортированные пресеты всегда нормализуются как пользовательские (`locked: false`) с новыми `id`;
  - в UI добавлен выбор режима импорта перед загрузкой JSON;
  - добавлен unit-тест на merge-импорт без потери текущих и с автопереименованием дублей.
- **Файлы:**
  - `web/src/store/telegramNotesStore.ts`
  - `web/src/store/telegramNotesStore.test.ts`
  - `web/src/routes/MasterAdminIncomingWorkspace.tsx`
  - `docs/sync/HERMES_HANDOFF.md`
- **Деплой / сервисы:** нет
- **Нужно в Cursor:**
  - быстро проверить UX текста режимов импорта (`merge/replace`) и при необходимости переименовать подписи на более короткие;
  - проверить на реальном JSON, что автосуффиксы имён (`(2)/(3)`) выглядят ожидаемо.
- **Риски:** общий `npm run build` по проекту всё ещё может блокироваться старым `HermesTerminalChat.tsx` (xterm/типы), к этому этапу не относится.

## 2026-05-17 17:14 (MSK) — Этап 7 для «ТГ заметок»: подтверждение удаления + экспорт/импорт + сброс

- **Ветка:** `hermes/work` @ `HEAD`
- **Сделано:**
  - добавлено подтверждение перед удалением пользовательского пресета;
  - добавлен экспорт пользовательских пресетов в JSON (`telegram-routing-presets.json`);
  - добавлен импорт пресетов из JSON с валидацией структуры и безопасной нормализацией;
  - добавлен «Сброс к базовым» (удаляет пользовательские пресеты, возвращает стандартные);
  - store расширен методами `exportPresets`, `importPresets`, `resetPresetsToDefault`;
  - добавлены unit-тесты на экспорт/импорт и сброс.
- **Файлы:**
  - `web/src/store/telegramNotesStore.ts`
  - `web/src/store/telegramNotesStore.test.ts`
  - `web/src/routes/MasterAdminIncomingWorkspace.tsx`
  - `docs/sync/HERMES_HANDOFF.md`
- **Деплой / сервисы:** нет
- **Нужно в Cursor:**
  - UX-проверка сценария «импорт поверх текущих пользовательских пресетов»;
  - при желании добавить merge-режим импорта (сейчас импорт заменяет пользовательские пресеты).
- **Риски:** `npm run build` по проекту всё ещё блокируется старым `HermesTerminalChat.tsx` (xterm/типы), не связано с ТГ заметками.

## 2026-05-17 17:02 (MSK) — Этап 6 для «ТГ заметок»: пользовательские пресеты + быстрые режимы

- **Ветка:** `hermes/work` @ `HEAD`
- **Сделано:**
  - добавлены базовые режимы: `Сбалансированный`, `Фокус на работе`, `Архивный режим`;
  - добавлен быстрый переключатель режимов: `Рабочий день / Разбор входящих / Архивный`;
  - добавлено управление пресетами в UI: создать, дублировать, переименовать, удалить;
  - удаление защищено для базовых пресетов (их нельзя случайно удалить);
  - store расширен под CRUD пресетов и quick-mode переключение;
  - добавлены unit-тесты на CRUD пресетов и быстрые режимы.
- **Файлы:**
  - `web/src/domain/telegramNotes.ts`
  - `web/src/store/telegramNotesStore.ts`
  - `web/src/store/telegramNotesStore.test.ts`
  - `web/src/routes/MasterAdminIncomingWorkspace.tsx`
- **Деплой / сервисы:** нет
- **Нужно в Cursor:**
  - UX-проверка управления пресетами на мобильной ширине;
  - при желании добавить подтверждение перед удалением пользовательского пресета.
- **Риски:** `npm run build` в проекте остаётся заблокирован старым `HermesTerminalChat.tsx` (xterm/типы), это вне модуля ТГ заметок.

## 2026-05-17 16:51 (MSK) — Этап 5 для «ТГ заметок»: умные пресеты авто-сортировки

- **Ветка:** `hermes/work` @ `29157f7`
- **Сделано:**
  - добавлены пресеты правил авто-сортировки (по приоритету, папке и состоянию по умолчанию);
  - добавлен выбор активного пресета в UI;
  - добавлено редактирование правил пресета прямо в интерфейсе (без правки кода);
  - добавлена кнопка «Применить правила сейчас» для быстрой массовой сортировки;
  - обновлён store: хранение пресетов, активного пресета и применение маршрутизации по правилам;
  - добавлен unit-тест применения авто-сортировки по активному пресету.
- **Файлы:**
  - `web/src/domain/telegramNotes.ts`
  - `web/src/store/telegramNotesStore.ts`
  - `web/src/store/telegramNotesStore.test.ts`
  - `web/src/routes/MasterAdminIncomingWorkspace.tsx`
- **Деплой / сервисы:** нет
- **Нужно в Cursor:**
  - проверить UX пресетов на реальном потоке заметок;
  - при желании добавить сохранение пользовательских пресетов с кастомным названием/дублированием.
- **Риски:** `npm run build` в проекте остаётся заблокирован старым `HermesTerminalChat.tsx` (xterm/типы), это вне модуля ТГ заметок.

## 2026-05-17 16:45 (MSK) — Этап 4 для «ТГ заметок»: ручная папка и массовые операции по выбранным

- **Ветка:** `hermes/work` @ `HEAD`
- **Сделано:**
  - добавлен выбор заметок (чекбоксы в карточках, подсветка выбранного);
  - добавлен блок **«Массовые операции»**: выбрать всё в текущем фильтре / снять выбор;
  - добавлены массовые действия по выбранным: статусы `В работу / В архив / Во входящие`;
  - добавлено ручное переопределение папки в карточке заметки (селект `Идеи/Работа/Финансы/Медиа/Разное`);
  - добавлено массовое перемещение выбранных заметок по папкам;
  - расширен store и добавлены unit-тесты для новых операций.
- **Файлы:**
  - `web/src/routes/MasterAdminIncomingWorkspace.tsx`
  - `web/src/store/telegramNotesStore.ts`
  - `web/src/store/telegramNotesStore.test.ts`
- **Деплой / сервисы:** нет
- **Нужно в Cursor:**
  - UX-проверка на длинных списках (удобство выбора/массовых действий);
  - при желании добавить «выбрать диапазон» и «инвертировать выбор».
- **Риски:** `npm run build` по-прежнему ломается на старом `HermesTerminalChat.tsx` (xterm/типы), к этой задаче не относится.

## 2026-05-17 16:36 (MSK) — Этап 3 для «ТГ заметок»: папки и массовая сортировка

- **Ветка:** `hermes/work` @ `HEAD`
- **Сделано:**
  - добавлены авто-папки заметок: `Идеи / Работа / Финансы / Медиа / Разное`;
  - добавлен фильтр по папкам и счётчики по каждой папке;
  - добавлена кнопка **«Массовая сортировка»** (автораскладка статусов по папкам/приоритету);
  - в карточке заметки теперь отображается папка;
  - расширены тесты парсера под этап 3.
- **Файлы:**
  - `web/src/domain/telegramNotes.ts`
  - `web/src/lib/telegramNotes.ts`
  - `web/src/lib/telegramNotes.test.ts`
  - `web/src/store/telegramNotesStore.ts`
  - `web/src/routes/MasterAdminIncomingWorkspace.tsx`
- **Деплой / сервисы:** нет
- **Нужно в Cursor:**
  - проверить UX на реальном Telegram export;
  - при необходимости подправить правила раскладки папок/массовой сортировки под ваши сценарии.
- **Риски:** `npm run build` всё ещё падает на старом `HermesTerminalChat.tsx` (xterm зависимости/типы), это вне текущего модуля.

## 2026-05-17 16:29 (MSK) — Этап 2 для «ТГ заметок»: умная чистка и быстрые действия

- **Ветка:** `hermes/work` @ `HEAD`
- **Сделано:**
  - добавлена дедупликация заметок при импорте Telegram JSON;
  - добавлены авто-теги (по контенту и доменам ссылок);
  - добавлен авто-приоритет (`Важно / Обычно / Позже`);
  - добавлены статусы заметки (`Входящие / В работе / Архив`) и быстрые кнопки действий в UI;
  - добавлены фильтры по статусу + расширенный поиск (текст/ссылки/теги);
  - расширены unit-тесты на дедуп/теги/приоритет.
- **Файлы:**
  - `web/src/domain/telegramNotes.ts`
  - `web/src/lib/telegramNotes.ts`
  - `web/src/lib/telegramNotes.test.ts`
  - `web/src/store/telegramNotesStore.ts`
  - `web/src/routes/MasterAdminIncomingWorkspace.tsx`
- **Деплой / сервисы:** нет
- **Нужно в Cursor:**
  - проверить UX на реальном export JSON из Telegram;
  - решить отдельно старые TS-проблемы `HermesTerminalChat.tsx` (они по-прежнему ломают `npm run build`).
- **Риски:** возможны конфликты при одновременных правках admin-UI в Cursor; логику тегов/приоритета стоит откалибровать на живых данных.

## 2026-05-17 — начальная фиксация (Дас)

- **Ветка на VPS:** `feat/deploy-v1-and-api-inspect` @ `99376d1` (ещё не на GitHub — у `hermes` нет push).
- **Сделано Hermes:** `scripts/deploy_v1.sh`, доработки `api/server.mjs`, `openapi.yaml`, smoke, `docs/environment/DEPLOY.md` и др.
- **Нужно в Cursor:** при слиянии сравнить с локальными `scripts/deploy/` и незакоммиченным UI Hermes; решить одну линию деплоя.
- **Настройки Hermes:** русский язык (`SOUL.md`, `display.language=ru`, `channel_prompts` для TG).

---

## 2026-05-17 — deploy key + единый деплой (Дас)

- **GitHub:** deploy key `hermes-vps-deploy`, ветка **`hermes/work`** на origin (push с VPS работает).
- **Слияние:** API inspect, smoke, `scripts/deploy/deploy.sh` (вместо голого `deploy_v1.sh`), правка пути `/v1/text/inspect` под nginx.
- **main:** слито с `hermes/work`, выкат через `deploy.sh`.

---

<!-- Новые блоки — только ВЫШЕ этой строки, новые сверху -->
